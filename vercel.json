{
  "builds": [
    {
      "src": "src/main.jsx",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "ls -R && vite build"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
